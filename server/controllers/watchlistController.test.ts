import httpMocks from 'node-mocks-http';
import query from '../models/model.js';
import watchlistController from './watchlistController.js';

describe('watchlistController', () => {
  describe('moveToWatched', () => {
    it('should move a movie from the watchlist table to the watched table', async () => {});
    it('should add a movie to the movies table if it is not already there', async () => {});
    
  });
});
